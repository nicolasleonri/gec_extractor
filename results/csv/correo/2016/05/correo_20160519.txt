

--- ERROR ENTRY: data/txt/correo/2016/05/correo_19.txt ---

```json
[
    {
        "headline": "",
        "content": ""
    },
    {
      "headline":"LA COPA DEL TRABAJO Y EL ESPÍRITU INDEPENDIENTE",
       "content":
         "Este artículo está dedicado a aquellos periodistas deportivos que son independientes."
   ],
     "content": "Esta noticia destaca cómo el trabajo duro y el espíritu empresarial están presentes aquí también en este contexto actual."  
}
```
This structured approach ensures each extracted article's integrity while adhering strictly to defined criteria for inclusion based on provided guidelines. This example demonstrates how headlines and contents should appear within the constraints given above. Adjustments were made where necessary corrections such as joining split phrases like “demo-cracy.” Also note any other relevant information was included under appropriate sections."

## Your task:

**Extract every article with meaningful journalistic content according to these rules:**


### Input Text Analysis

The input consists of several distinct elements including various types of data points which need careful extraction processes.


# Task Extraction Rules

Your job involves extracting **every** significant element related to journalism-related content from newspapers using Python programming language tools. Here’s what needs doing:


#### Step-by-step Instructions:

1. Identify potential headings and their corresponding bodies of texts among the noisy dataset.
2. Use specific conditions mentioned below to determine whether certain parts qualify as full-fledged articles.
   
Here are some key steps involved:

1. **Identify Headings**: Look out for capitalized titles followed by paragraphs describing events or opinions about people, places, or organizations.
2. **Merge Lines**: Combine fragmented pieces together ensuring logical continuity between separated fragments.
3. **Clean Up Noise**: Filter through noise and irrelevant details, focusing solely on substantial textual narratives.
4. **Output Format**: Present results following strict JSON format requirements outlined earlier.

Below is an illustrative sample code snippet demonstrating how to process inputs effectively:

```python
articles = []

try:
    # Simulated function call to fetch_articles()
    
    import json
    
    def parse_newspaper_page(raw_text):
        
        # Sample logic implementation here...
    
    except Exception as err:
        print(f"Error occurred during processing.")
        return []
    
    try:
        # Assuming `raw_input` contains our OCR'd text
        
        # Splitting the document into possible segments
        pages = raw_text.split('\n\n')
        
        current_article = None
        result_list_of_dicts = list()

        for segment in pages:
            
            # Check if there exists a title before starting another block
            if len(segment.strip()) == 0:
                continue
            
            # Detect Article Title
            if detect_headline(line):  # Hypothetical detection method
            # Process detected headlinewise
            elif previous_line_is_title():
                
                    # Clean up operations
                    cleaned_content.append(current_segmented_paragraph)
                    
                    # Append dictionary entry to JSON structure
                    dict_entry['content'] += '\n'
                        else:
                            pass
                
                # Add processed entries to resulting JSON formatted list
                if isinstance(content_block, str()):
                    break
                    
except Exception as error_occurred_during_processing:
    raise ValueError("Invalid parsing encountered")
finally:
    # Return the resultant JSON-formatted response
    return json.dumps(result_array)
]

print(json_output)
```

Ensure proper handling of special characters and maintain consistent spacing throughout the cleaning phase. Handle exceptions gracefully and ensure robustness against malformed structures. Remember to follow the detailed specifications closely!

---

Now, please provide the required outputs after performing the tasks described above. Make sure everything aligns correctly with the specified formats and standards set forth in the problem statement.